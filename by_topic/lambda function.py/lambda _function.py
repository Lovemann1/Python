# lambda funtion in python is a small, anonymous functon defined using the lambda keyword.
# it can have any number of arguments, but only one expression, which is evaluated and returned.
# lambda functon are often used for short, simple operatins without needing to define a full function using def.


add = lambda x,y: x+y
print(add(3,5)) 
